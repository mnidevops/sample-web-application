currentBuild.displayName = "MNI_Pipeline # "+currentBuild.number
pipeline
{
  agent
  {
    docker
    {
      image 'maven'
      args '-v $HOME/.m2:/root/.m2'
     }
    } 
  stages 
  {
   stage ('Say Hi!')
     {
        steps
        {
          sh 'echo hi'
         }
     }
  }
}
